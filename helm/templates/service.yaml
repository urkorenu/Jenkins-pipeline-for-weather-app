apiVersion: v1
kind: Service
metadata:
  name: weather-service
spec:
  selector:
    app: weather-app
    service: weather
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: mail-service
spec:
  selector:
    app: weather-app
    service: mail
  ports:
    - protocol: TCP
      port: 5002
      targetPort: 5002
  type: ClusterIP

